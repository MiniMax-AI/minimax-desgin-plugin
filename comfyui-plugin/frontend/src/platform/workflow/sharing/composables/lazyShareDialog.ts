export function prefetchShareDialog() {
  // Sharing is a cloud account feature and is intentionally unavailable.
}

export async function openShareDialog() {
  // Keep the async API for callers, but never open a login/share flow.
}
